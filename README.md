# Projeto AWS Flask + Docker + Terraform

Projeto completo de API Flask usando Docker, Terraform na AWS, front-end básico e testes locais com ngrok.

## Estrutura
- app.py, database.py, models.py
- Dockerfile, requirements.txt
- static/, templates/
- terraform/ (infraestrutura AWS)

## Tecnologias
- Python 3 + Flask
- Docker
- Terraform
- AWS EC2 + RDS
- HTML/CSS
- ngrok

## Passo a passo
1. Criar virtualenv e instalar dependências:

```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt

